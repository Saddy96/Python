#Random No Generaton:
import random

'''It doesn't have a random not
gerarator function like random(), python has a
built-in module to generate random no,use random'''

#Generate any no between 1 and 10
print(random.randrange(1,10)) 